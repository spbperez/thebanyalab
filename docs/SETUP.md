# Порядок запуска

## Статус

Каркас готов, данные не собраны. `npm run build` **намеренно падает** на
незаполненных полях `client.json` — это защита от выкатки сайта с дырами.
Список того, чего не хватает, печатает сама сборка.

## 1. Данные

Закрыть `docs/INTAKE.md` целиком. Неполный чеклист — не начинать.

## 2. Домен и Cloudflare

**Оформляются на владельца бизнеса, доступ выдаётся подрядчику.** До кода.
Домен на подрядчике — самая дорогая ошибка в этом жанре: она всплывает через
год, когда разговор о ней уже неудобный.

## 3. Репозиторий и Pages

    gh repo create thebanyalab --private --source=. --push

Cloudflare Pages → Create project → Connect to Git → репозиторий.
Build command: `npm run build`. Output directory: `site`.
Затем Custom domains → добавить домен.

## 4. Переменные окружения

В Cloudflare Pages → Settings → Environment variables:

    TELEGRAM_BOT_TOKEN   от @BotFather
    TELEGRAM_CHAT_ID     id чата владельца (через @userinfobot)
    RESEND_API_KEY       resend.com, бесплатного тарифа хватает
    FROM_EMAIL           адрес на своём домене, подтверждённый в Resend
    OWNER_EMAIL          куда дублировать заявки

**Оба канала обязательны.** Без базы заявка живёт только в момент отправки:
если единственный канал упал, заявка потеряна и никто об этом не узнает.

## 5. Оплата

Stripe на компанию владельца (нужен EIN и счёт компании).
Statement descriptor выставить в `BANYA LAB` — именно это видит клиент
в выписке по карте, и именно этого ему сейчас не хватает для доверия.

Payment Links создаются в панели Stripe, без кода:

    Lab session · Master Yura     $175, quantity 1–4
    Lab session · Master Max      $250, quantity 1–4
    Ocean session                 фиксированная сумма за группу

Ссылки кладутся в `client.json` → `pay.links`, страница `/pay/` закрыта от
индексации и не стоит в меню: её отдают уже подтверждённому гостю.

**Календаря на сайте нет и не планируется на этом уровне.** Доступность живёт
в голове мастера, в погоде и в расписании лодки. Календарь, который врёт,
хуже отсутствующего. Когда поток вырастет и владелец начнёт терять заявки
из-за скорости ответа — это будет сигнал, и тогда календарь заводится
отдельным ТЗ, по настоящим данным, а не по рассказу.

## 6. Перед сдачей

- `npm run build && npm run check` — зелено
- посмотреть первый экран на настоящем телефоне, не в эмуляторе
- отправить заявку с телефона владельца и убедиться, что она пришла в оба канала
- сверить NAP с Google Business Profile руками, символ в символ
- проверить, что Stripe-ссылка открывается и показывает название компании
